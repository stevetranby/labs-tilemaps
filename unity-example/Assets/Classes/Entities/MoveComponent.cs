using UnityEngine;
using System.Collections;

namespace ST
{
    /// <summary>
    /// Move component used for both AIComponent and InputComponent (or possibly others).
    /// It controls defining paths, waypoints, goals, and movement behavior at a lower level.
    /// </summary>
    public class MoveComponent : MonoBehaviour
    {

        // Use this for initialization
        void Start ()
        {
    
        }
    
        // Update is called once per frame
        void Update ()
        {
    
        }
    }
}